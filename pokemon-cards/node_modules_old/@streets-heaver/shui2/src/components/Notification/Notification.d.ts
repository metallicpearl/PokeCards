import type { ReactElement, ReactNode } from 'react';
import type { ButtonProps } from '../Button/Button';
import type { MenuProps } from '../Menu/Menu';
import type { ToProps } from '../NavLink/NavLink';
type NotificationProps = {
    title?: ReactNode;
    time?: ReactNode;
    content?: ReactNode;
    titleIcon: ReactNode;
    funcButtonProps: ButtonProps;
    settingsOptions: MenuProps['items'];
    read?: boolean;
    to?: ToProps | null;
};
export declare const Notification: (props: NotificationProps) => ReactElement;
export {};

import type { ReactElement, ReactNode } from 'react';
export type SpinnerProps = {
    text?: ReactNode;
    textPosition?: 'top' | 'right' | 'bottom' | 'left';
    size?: 'icon' | 'tiny' | 'extra-small' | 'small' | 'medium' | 'large' | 'extra-large' | 'huge';
    type?: 'primary' | 'subtle';
    automationId?: string;
};
export declare const Spinner: (props: SpinnerProps) => ReactElement;

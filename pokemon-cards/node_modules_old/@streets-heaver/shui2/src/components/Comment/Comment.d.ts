import type { ReactElement, ReactNode } from 'react';
export type CommentProps = {
    limitContent?: boolean;
    source?: {
        name?: string;
        app?: string;
        time?: string;
    };
    avatar?: ReactNode;
    content?: ReactNode;
    actions?: ReactNode;
    automationId?: string;
};
export declare const Comment: (props: CommentProps) => ReactElement;

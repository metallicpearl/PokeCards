import type { ReactElement, ReactNode } from 'react';
import type { ToProps } from '../NavLink/NavLink';
import { ButtonProps } from '../Button/Button';
export type MessageBarProps = {
    fullWidth?: boolean;
    type?: 'success' | 'warning' | 'info' | 'error';
    title?: ReactNode;
    body?: ReactNode;
    link?: {
        to: ToProps;
        content?: ReactNode;
    };
    actions?: MessageBarActions;
    automationId?: string;
};
type MessageBarActions = {
    action1?: {
        onClick: ButtonProps['onClick'];
        content: ReactNode;
    };
    action2?: {
        onClick: ButtonProps['onClick'];
        content: ReactNode;
    };
    onCloseButtonClick?: (e: ButtonProps['onClick']) => void;
};
export declare const MessageBar: (props: MessageBarProps) => ReactElement;
export {};

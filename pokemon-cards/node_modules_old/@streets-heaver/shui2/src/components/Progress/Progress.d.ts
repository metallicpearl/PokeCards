import type { ReactElement } from 'react';
export type ProgressProps = {
    value: number;
    max?: number;
    size?: 'medium' | 'large';
    type?: 'static' | 'animated' | 'indeterminate';
    status?: 'default' | 'success' | 'warning' | 'error';
};
export declare const Progress: (props: ProgressProps) => ReactElement;

import type { CSSProperties, ReactElement, ReactNode } from 'react';
import type { LinkProps, To } from 'react-router-dom';
export type NavLinkProps = {
    to?: ToProps;
    type?: 'default' | 'default-inline' | 'subtle' | 'over-brand';
    style?: CSSProperties;
    children?: ReactNode;
    newTab?: boolean;
    reloadDocument?: boolean;
    state: LinkProps['state'];
};
export type ToProps = To;
export declare const NavLink: (props: NavLinkProps) => ReactElement;

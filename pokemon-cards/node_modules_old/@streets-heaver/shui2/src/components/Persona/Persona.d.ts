import type { AvatarProps } from '../Avatar/Avatar';
import type { ReactElement } from 'react';
import type { PresenceBadgeProps } from '../PresenceBadge/PresenceBadge';
export type PersonaProps = {
    primaryText?: string;
    number?: {
        text: string;
        link: string;
    };
    secondaryText?: string;
    tertiaryText?: string;
    quaternaryText?: string;
    icon?: {
        type: 'avatar';
        props: AvatarProps;
    } | {
        type: 'presence';
        props: PresenceBadgeProps;
    } | null;
    size?: 'tiny' | 'extra-small' | 'small' | 'medium' | 'large';
    side?: 'after' | 'below' | 'before';
    alignment?: 'center' | 'start';
    customWidth?: string | number;
    mobile?: boolean;
    ghost?: boolean;
    automationId?: string;
};
export declare const Persona: (props: PersonaProps) => ReactElement;

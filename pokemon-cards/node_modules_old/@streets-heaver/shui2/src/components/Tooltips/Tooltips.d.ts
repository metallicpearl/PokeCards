import type { ReactElement, ReactNode } from 'react';
import type { PlacesType } from 'react-tooltip';
export type TooltipProps = {
    id: boolean;
    tooltips: attributes[];
    noPadding: boolean;
    width: string;
    maxWidth: string;
};
export type attributes = {
    anchorSelect: string;
    place: PlacesType;
    content: ReactNode;
    copy: boolean;
    isOpen: boolean;
    setIsOpen: () => void;
    openEvents: Record<string, boolean>;
    closeEvents: Record<string, boolean>;
    globalCloseEvents: Record<string, boolean>;
    delayShow: number;
    delayHide: number;
};
export declare const Tooltips: (props: TooltipProps) => ReactElement;

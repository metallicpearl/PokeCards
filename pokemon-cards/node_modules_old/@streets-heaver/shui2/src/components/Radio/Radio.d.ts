import type { ReactElement, ReactNode } from 'react';
export type RadioProps = {
    items: RadioItem[];
    defaultSelected?: string;
    onChange?: (item: RadioItem) => void;
};
interface RadioItem {
    id: string;
    content: ReactNode;
}
export declare const Radio: (props: RadioProps) => ReactElement;
export {};

import type { ReactNode, ReactElement } from 'react';
export type SegmentedControlProps = {
    segments: Segment[];
    groupName?: string;
    defaultSegment?: string | number;
    onChange?: (value: string | number) => void;
    type?: 'tabs' | 'pills' | 'switch';
    updateSelectedSegment?: string | number;
    automationId?: string;
};
interface Segment {
    value: string | number;
    icon?: ReactNode;
    label?: string;
}
export declare const SegmentedControl: (props: SegmentedControlProps) => ReactElement;
export {};

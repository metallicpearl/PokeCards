import type { CSSProperties, ReactElement, ReactNode } from 'react';
export type BadgeProps = {
    text?: ReactNode;
    icon?: ReactNode;
    rounded?: boolean;
    size?: 'chip' | 'medium' | 'large' | 'larger';
    type?: 'filled' | 'tint' | 'outline' | 'ghost';
    color?: 'brand' | 'danger' | 'severeWarning' | 'warning' | 'success' | 'important' | 'informative' | 'subtle';
    style?: CSSProperties;
    automationId?: string;
};
export declare const Badge: (props: BadgeProps) => ReactElement;

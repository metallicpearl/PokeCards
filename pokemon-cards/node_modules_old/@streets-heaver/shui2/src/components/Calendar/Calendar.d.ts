import type { ReactElement } from 'react';
export type CalendarProps = {
    date?: Date;
    onSelected?: (date: Date) => void;
    unavailableDates?: {
        date: Date;
        hasAvailability: boolean;
    }[];
    onMonthViewChange?: (month: number, year: number) => void;
    automationId?: string;
    visible?: boolean;
    type?: 'transparent';
    firstAvailableDate?: Date;
};
export declare const Calendar: (props: CalendarProps) => ReactElement;

import type { CSSProperties, ReactElement, ReactNode } from 'react';
export type ToastProps = {
    icon: 'success' | 'warning' | 'information' | 'error' | 'event' | 'spinner' | 'upload' | 'download' | ReactNode;
    onCloseButtonClick?: () => void;
    title?: string;
    subtitle?: string;
    children?: ReactNode;
    progress?: number;
    progressType?: 'static' | 'animated' | 'indeterminate';
    progressStatus?: 'default' | 'success' | 'warning' | 'error';
    action1?: string;
    action1OnClick?: () => void;
    action2?: string;
    action2OnClick?: () => void;
    style?: CSSProperties;
    automationId?: string;
};
export declare const Toast: (props: ToastProps) => ReactElement;
